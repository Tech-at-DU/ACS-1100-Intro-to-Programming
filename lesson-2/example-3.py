# Challenge: Use f string to print and format the following: 

score = 53978000
level = 16
name = "Mario"

# Below I have used print to display a message. Your job is 
# to display the same message using an f string:

print(str(name) + " scored " + str(score) + " points and completed level " + str(level))